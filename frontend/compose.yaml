services:
  client:
    build:
      dockerfile: Dockerfile
    ports:
      - '5173:5173'
    volumes:
      - ./:/srv/http/
      - /srv/http/node_modules
    environment:
      NODE_ENV: development
      APP_PORT: 5173
