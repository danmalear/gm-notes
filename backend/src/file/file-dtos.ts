/**
 * Minimal response structure for file records.
 * Useful for showing basic information in a list or as part of a parent object.
 */
export interface FileStub {
	fileId: string;
	fileName: string;
}
