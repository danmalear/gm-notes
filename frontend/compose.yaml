services:
  client:
    build:
      dockerfile: Dockerfile
    ports:
      - '8080:8080'
    volumes:
      - ./:/srv/http/
      - /srv/http/node_modules
    environment:
      NODE_ENV: development
      APP_PORT: 8080
